type Event = {
    id: number,
    numEvent: string,
    title: string,
    category: string,
    categoryAge: string,
    cost: number,
    dateEvent: string,
    imageUrl: string
}

export default Event;