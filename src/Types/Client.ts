export type Client = {
    id: number,
    codecli: string,
    lastName: string,
    firstName: string,
    email: string,
    roles: string | string[],
    password: string,
    phone: string,
    cardNumber: string
}